<Window x:Class="ChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatClient"
        mc:Ignorable="d"
        Title="Telecommunication Chat System - Client" Height="520" Width="950">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Username:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="UsernameBox" Width="120" Margin="0,0,10,0"/>

            <TextBlock Text="Password:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <PasswordBox x:Name="PasswordBox" Width="120" Margin="0,0,10,0"/>

            <Button x:Name="SignUpButton" Content="Sign Up" Width="100" Margin="0,0,10,0" Click="SignUp_Click"/>
            <Button x:Name="LoginButton" Content="Login" Width="100" Click="Login_Click"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="ChatBox"
                     Grid.Column="0"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     TextWrapping="Wrap"
                     Margin="0,0,10,0"/>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Online Users"
                           FontWeight="Bold"
                           Margin="0,0,0,5"/>
                <Button Content="Public Chat"
                        Height="30"
                        Margin="0,0,0,5"
                        Click="PublicChat_Click"/>
                <ListBox x:Name="UsersListBox" Height="300"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBox x:Name="MessageInput" Width="640" Margin="0,0,10,0"/>
            <Button x:Name="SendButton" Content="Send" Width="100" Margin="0,0,10,0" Click="Send_Click" IsEnabled="False"/>
            <Button x:Name="SendFileButton" Content="Send File" Width="100" Click="SendFile_Click" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>